name: korserver

services:
  korserver:
    build:
      context: .
    image: korserver:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      KORSERVER_CONFIG: /etc/korserver/config.yaml
    cap_add:
      - NET_ADMIN
      - NET_RAW
    network_mode: host
    devices:
      - /dev/net/tun:/dev/net/tun
      # Optional accelerated TUN path for openconnect. Docker refuses to start
      # the container if a listed device is missing on the host, so enable it
      # only when /dev/vhost-net exists (`modprobe vhost_net`).
      # - /dev/vhost-net:/dev/vhost-net
    volumes:
      - ./config:/etc/korserver
      - ./data:/var/lib/korserver
      - ./logs:/var/log/korserver
